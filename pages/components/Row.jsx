import React from 'react';
import Box from './Box.jsx';

const Row = (props) => {
  const { Boardsize } = props;
  return (
    <div>
      <p>This is the row</p>
    </div>
  );
};

export default Row;
